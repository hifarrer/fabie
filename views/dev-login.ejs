<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Development Access • FABIE</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background: var(--bg, #f5f5f5);
    }
    .login-container {
      max-width: 400px;
      width: 100%;
      padding: 24px;
    }
    .login-card {
      background: white;
      border-radius: 8px;
      padding: 32px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .login-card h1 {
      margin: 0 0 8px 0;
      font-size: 24px;
    }
    .login-card p {
      color: #666;
      margin: 0 0 24px 0;
    }
    .error {
      background: #fee;
      color: #c33;
      padding: 12px;
      border-radius: 4px;
      margin-bottom: 16px;
      border: 1px solid #fcc;
    }
    .login-card form {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }
    .login-card label {
      font-weight: 600;
      display: block;
      margin-bottom: 8px;
    }
    .login-card input {
      width: 100%;
      padding: 12px;
      border: 1px solid #ddd;
      border-radius: 4px;
      font-size: 16px;
      box-sizing: border-box;
    }
    .login-card button {
      padding: 12px 24px;
      background: #0066cc;
      color: white;
      border: none;
      border-radius: 4px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
    }
    .login-card button:hover {
      background: #0052a3;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-card">
      <h1>Development Access</h1>
      <p>This site is currently under development. Please enter the password to continue.</p>
      
      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } %>

      <form method="POST" action="/dev-login<%= typeof redirect !== 'undefined' && redirect ? '?redirect=' + encodeURIComponent(redirect) : '' %>">
        <div>
          <label for="password">Password</label>
          <input 
            type="password" 
            id="password" 
            name="password" 
            placeholder="Enter password"
            required
            autofocus
          />
        </div>
        <button type="submit">Access Site</button>
      </form>
    </div>
  </div>
</body>
</html>
